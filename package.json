{
    "name": "routemaster-root",
    "private": true,
    "version": "1.0.0",
    "workspaces": [
        "frontend",
        "bff",
        "backend"
    ],
    "scripts": {
        "dev": "concurrently --kill-others -n \"BFF,FRONT,BACK\" -c \"blue,green,red\" \"npm run dev:bff\" \"npm run dev:frontend\" \"npm run dev:backend\"",
        "predev": "node scripts/dev-cleanup.js",
        "dev:frontend": "wait-on http://127.0.0.1:8080/api/config && npm run dev --prefix frontend",
        "dev:bff": "npm run dev --prefix bff",
        "dev:backend": "npm run start --prefix backend",
        "build": "npm run build --prefix frontend",
        "start": "npm run start --prefix bff",
        "install:all": "npm install && npm install --prefix frontend && npm install --prefix bff && npm install --prefix backend"
    },
    "devDependencies": {
        "concurrently": "^8.2.2",
        "wait-on": "^9.0.4"
    }
}